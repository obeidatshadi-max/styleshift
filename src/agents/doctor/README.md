# doctor agent

AI Doctor: role-plays the physician. Will wrap buildJudgePrompt/parseJudgeResponse from lib/voice-partner-core.

Status: wired — live via /api/simulation/{start,message,end} and TextSimulation.tsx.
